version: '2.1'
rules:
  - id: 1
    name: rule1
    tags:
      type: flow1
      category: category1
    conditions:
      - operator: expression
        parameters:
          inputs:
            - address: headers
          code: input.startsWith("Arachni") && input.endsWith("v1")
