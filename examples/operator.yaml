version: '2.1'
rules:
  - id: ada54ac7-4cef-436c-9ea4-a47a35c6db40
    name: operator-expression
    tags:
      type: security_scanner
      category: attack_attempt
    conditions:
      - operator: expression
        parameters:
          inputs:
            - address: headers
          code: input.startsWith("Arachni") && input.endsWith("v1")
