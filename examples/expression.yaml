version: '2.1'
rules:
  - id: 1
    name: rule1
    tags:
      type: flow1
      category: category1
    expression:
      code: headers.user_agent == "Arachni/v1" && size(query.filter(x, x.endsWith("honeypot"))) > 0
      inputs:
        - address: headers
        - address: query
