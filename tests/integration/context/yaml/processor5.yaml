version: '2.1'
rules:
  - id: 1
    name: rule1
    tags:
      type: flow1
      category: category1
    conditions:
      - operator: match_regex
        parameters:
          inputs:
            - address: param
              key_path:
                - x
            - address: param
              key_path:
                - y
          regex: Sqreen
  - id: 2
    name: rule2
    tags:
      type: flow2
      category: category2
    conditions:
      - operator: match_regex
        parameters:
          inputs:
            - address: param
              key_path:
                - z
          regex: Sqreen

