version: '1.0'
events:
  - id: 1
    tags:
      type: flow1
    conditions:
      - operation: match_regex
        parameters:
          inputs:
            - param
          regex: Sqreen
          options:
            case_sensitive: true
      - operation: match_regex
        parameters:
          inputs:
            - param2
          regex: Sqreen
    action: record

