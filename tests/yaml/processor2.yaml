version: '2.1'
rules:
  - id: 1
    tags:
      type: flow1
    conditions:
      - operator: match_regex
        parameters:
          inputs:
            - address: param
          regex: Sqreen
          options:
            case_sensitive: true
      - operator: match_regex
        parameters:
          inputs:
            - address: param2
          regex: Sqreen

