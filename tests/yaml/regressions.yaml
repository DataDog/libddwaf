version: '2.1'
rules:
  - id: 1
    name: rule1
    tags:
      type: test_rule
      category: category1
    conditions:
      - operator: match_regex
        parameters:
          inputs:
            - address: value
          regex: .*
