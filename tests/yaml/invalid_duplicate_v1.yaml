version: '1.1'
events:
  - id: "1"
    name: rule1
    tags:
      type: type1
      category: category1
    conditions:
      - operation: match_regex
        parameters:
          inputs:
            - arg1
          regex: .*
      - operation: match_regex
        parameters:
          inputs:
            - arg2
          regex: .*
  - id: "1"
    name: rule2
    tags:
      type: type1
      category: category1
    conditions:
      - operation: match_regex
        parameters:
          inputs:
            - arg1
          regex: .*
      - operation: match_regex
        parameters:
          inputs:
            - arg2
          regex: .*
