version: '2.1'
rules:
  - id: 1
    tags:
      type: flow1
    conditions:
      - operator: match_regex
        parameters:
          inputs:
            - address: value1
            - address: value2
          regex: rule1
  - id: 2
    tags:
      type: flow2
    conditions:
      - operator: match_regex
        parameters:
          inputs:
            - address: value1
          regex: rule2
  - id: 3
    tags:
      type: flow2
    conditions:
      - operator: match_regex
        parameters:
          inputs:
            - address: value2
          regex: rule3

