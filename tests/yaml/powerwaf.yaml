version: '2.1'
rules:
  - id: 1
    tags:
      type: security_scanner
    conditions:
      - operator: match_regex
        parameters:
          inputs:
            - address: value
          regex: rule1

