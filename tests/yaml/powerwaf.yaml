version: '1.0'
events:
  - id: 1
    tags:
      type: security_scanner
    conditions:
      - operation: match_regex
        parameters:
          inputs:
            - value
          regex: rule1
    action: record

