version: '1.0'
events:
  - id: 1
    tags:
      type: test_coverage
    conditions:
      - operation: match_regex
        parameters:
          inputs:
            - arg
          regex: .*
    transformers:
        - removeNulls
    action: record

