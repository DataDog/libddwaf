version: '2.1'
rules:
  - id: 1
    tags:
      type: test_coverage
    conditions:
      - operator: match_regex
        parameters:
          inputs:
            - address: arg
          regex: .*
    transformers:
        - removeNulls

