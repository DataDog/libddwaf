version: '2.1'
rules:
  - id: 1
    tags:
      type: flow1
    conditions:
      - operator: match_regex
        parameters:
          inputs:
            - address: param1
          regex: Sqreen
  - id: 2
    tags:
      type: flow2
    conditions:
      - operator: match_regex
        parameters:
          inputs:
            - address: param2
          regex: Sqreen

