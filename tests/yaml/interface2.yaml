version: '1.0'
events:
  - id: 1
    tags:
      type: flow1
    conditions:
      - operation: match_regex
        parameters:
          inputs:
            - value1
          regex: rule2
      - operation: match_regex
        parameters:
          inputs:
            - value2
          regex: rule3
    action: record
