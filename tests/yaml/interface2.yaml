version: '2.1'
rules:
  - id: 1
    tags:
      type: flow1
    conditions:
      - operator: match_regex
        parameters:
          inputs:
            - address: value1
          regex: rule2
      - operator: match_regex
        parameters:
          inputs:
            - address: value2
          regex: rule3
