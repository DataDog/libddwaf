{
  name: "Validate that base rules still match when there's no contention",
  runs: [
    {
      persistent-input: {
        base-rule2-input: admin
      },
      rules: [
        {
          2: [
            {
              address: base-rule2-input,
              value: admin
            }
          ]
        }
      ],
      code: match
    }
  ]
}
